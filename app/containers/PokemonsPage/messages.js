/*
 * PokemonsPage Messages
 *
 * This contains all the text for the PokemonsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PokemonsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PokemonsPage container!',
  },
});
