// import { selectPokemonsPageDomain } from '../selectors';

describe('selectPokemonsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
