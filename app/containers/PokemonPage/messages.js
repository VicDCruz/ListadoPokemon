/*
 * PokemonPage Messages
 *
 * This contains all the text for the PokemonPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PokemonPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PokemonPage container!',
  },
});
